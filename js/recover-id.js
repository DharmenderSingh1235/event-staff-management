document.querySelector('#recoverForm')?.addEventListener('submit',e=>{e.preventDefault();const q=document.querySelector('#contactInput').value.trim().toLowerCase(),matches=getBookings().filter(b=>b.phone.toLowerCase()===q||b.email.toLowerCase()===q),out=document.querySelector('#recoverResult');out.innerHTML=matches.length?matches.map(b=>`<div class="result-item"><span class="status ${statusClass(b.status)}">${b.status}</span><h3>${escapeHTML(b.eventType)} · ${escapeHTML(b.eventDate)}</h3><p class="meta">Request ID: <b>${escapeHTML(b.requestID)}</b></p><a href="check-status.html">Check this booking</a></div>`).join(''):'<div class="result-item">No bookings match that phone number or email.</div>'})
