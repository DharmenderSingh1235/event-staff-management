document.head.insertAdjacentHTML('beforeend','<link rel="stylesheet" href="css/payment.css">');
const PAYMENT_UPI_ID='8853535112-4@ybl', PAYMENT_PHONE='88535 35112';
function paymentBooking(){return findBooking(new URLSearchParams(location.search).get('id')||localStorage.getItem('latestRequestID'))}
function upiLink(b){return `upi://pay?pa=${encodeURIComponent(PAYMENT_UPI_ID)}&pn=EventEase&am=${b.amount}&cu=INR&tn=${encodeURIComponent('Booking '+b.requestID)}`}
function markPaid(b){const all=getBookings(),record=all.find(x=>x.requestID===b.requestID);record.paymentStatus='Paid';record.paymentMethod='UPI';record.paidAt=new Date().toISOString();saveBookings(all);localStorage.setItem('latestReceiptID',record.requestID);location.href='receipt.html?id='+encodeURIComponent(record.requestID)}
function renderPayment(){const box=document.querySelector('#paymentContent');if(!box)return;const b=paymentBooking();if(!b){box.innerHTML='<h2>Booking not found</h2>';return}if(b.status!=='Confirmed'){box.innerHTML='<h2>Payment is not available yet</h2><p class="muted">The admin must confirm this booking first.</p>';return}if(b.paymentStatus==='Paid'){box.innerHTML=`<h2>Payment already received</h2><a class="button" href="receipt.html?id=${encodeURIComponent(b.requestID)}">View receipt</a>`;return}const qr=`https://api.qrserver.com/v1/create-qr-code/?size=220x220&data=${encodeURIComponent(upiLink(b))}`;box.innerHTML=`<div class="checkout-top"><div><p class="eyebrow">SECURE DEMO CHECKOUT</p><h2>Pay ₹${Number(b.amount).toLocaleString('en-IN')}</h2><p class="meta">${escapeHTML(b.eventType)} · ${b.requestID}</p></div><span class="secure-badge">🔒 Secure</span></div><div class="checkout-layout"><div><h3>Choose payment method</h3><label class="payment-choice selected"><input type="radio" name="method" checked> <span><b>UPI</b><small>Google Pay, PhonePe, Paytm & bank apps</small></span><strong>UPI</strong></label><label class="payment-choice disabled"><input type="radio" disabled> <span><b>Cards</b><small>Coming soon</small></span></label><label class="payment-choice disabled"><input type="radio" disabled> <span><b>Netbanking</b><small>Coming soon</small></span></label><div class="app-row"><span>Pay with:</span><button type="button" data-app="Google Pay">G Pay</button><button type="button" data-app="PhonePe">PhonePe</button><button type="button" data-app="Paytm">Paytm</button></div><button id="openUpi" class="pay-button">Continue to UPI app →</button><p class="muted tiny">On a phone, this opens your installed UPI payment app. On a computer, scan the QR code.</p></div><aside class="qr-panel"><img src="${qr}" alt="UPI payment QR code"><b>Scan to pay</b><span>${PAYMENT_UPI_ID}</span><a href="tel:+919876543210">Need help? +91 ${PAYMENT_PHONE}</a></aside></div><details class="demo-note"><summary>Demo mode notice</summary><p>This screen copies a payment-gateway checkout flow. It cannot confirm real payments until a payment gateway and backend are connected.</p><button id="demoPaid" class="secondary small">Demo: mark payment complete</button></details>`;const open=()=>{location.href=upiLink(b)};document.querySelector('#openUpi').onclick=open;box.querySelectorAll('[data-app]').forEach(btn=>btn.onclick=open);document.querySelector('#demoPaid').onclick=()=>markPaid(b)}
function renderReceipt(){const box=document.querySelector('#receiptContent');if(!box)return;const b=paymentBooking()||findBooking(localStorage.getItem('latestReceiptID'));box.innerHTML=b&&b.paymentStatus==='Paid'?`<p class="eyebrow">EVENTEASE</p><h1>Payment receipt</h1><p class="meta">Paid on ${new Date(b.paidAt).toLocaleString()} via UPI</p><hr><p><b>Request ID</b><br>${escapeHTML(b.requestID)}</p><p><b>Customer</b><br>${escapeHTML(b.name)} · ${escapeHTML(b.email)}</p><p><b>Event</b><br>${escapeHTML(b.eventType)} on ${escapeHTML(b.eventDate)} at ${escapeHTML(b.location)}</p><hr><h2>Total paid: ₹${Number(b.amount).toLocaleString('en-IN')}</h2><span class="status paid">Paid</span>`:'<h2>No paid receipt found.</h2>'}
renderPayment();renderReceipt();
