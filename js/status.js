document.querySelector('#statusForm')?.addEventListener('submit',e=>{e.preventDefault();const b=findBooking(document.querySelector('#requestInput').value),out=document.querySelector('#statusResult');if(!b){out.innerHTML='<div class="result-item">No booking was found with that Request ID.</div>';return}out.innerHTML=`<div class="result-item"><span class="status ${statusClass(b.status)}">${escapeHTML(b.status)}</span><h3>${escapeHTML(b.eventType)} · ${escapeHTML(b.eventDate)}</h3><p class="meta">${escapeHTML(b.location)} · ${b.guests} guests</p><p class="meta">Request ID: <b>${escapeHTML(b.requestID)}</b></p>${b.status==='Confirmed'?`<a class="button" href="payment.html?id=${encodeURIComponent(b.requestID)}">${b.paymentStatus==='Paid'?'View receipt':'Make payment'}</a>`:''}</div>`})
